name: Build Kernel - Ultra Fast

on:
  workflow_dispatch:
    inputs:
      defconfig:
        description: '内核配置文件'
        required: true
        default: 'k69v1_64_k419_defconfig'

env:
  ARCH: arm64
  SUBARCH: arm64
  CROSS_COMPILE: aarch64-linux-gnu-

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120  # 延长到2小时

    steps:
    - name: 检出仓库
      uses: actions/checkout@v4

    - name: 极速编译内核
      run: |
        echo "=== 🚀 极速内核编译启动 ==="
        
        # 性能配置
        CPU_CORES=$(nproc)
        PARALLEL_JOBS=$((CPU_CORES * 3))  # 3倍核心数，平衡性能
        
        echo "配置: $PARALLEL_JOBS 并行任务 | $ARCH架构"
        
        # 极简依赖安装
        echo "📦 安装核心依赖..."
        sudo apt-get update -y
        sudo apt-get install -y --no-install-recommends \
          build-essential bc bison flex libssl-dev libelf-dev \
          gcc-aarch64-linux-gnu git python3 libncurses-dev
        
        sudo ln -sf /usr/bin/python3 /usr/bin/python
        
        # 直接克隆到当前目录
        echo "🔧 获取内核源码..."
        git clone --depth=1 --single-branch --branch=mt6768-4.19 \
          https://github.com/WUNELEZI1/Gale-Kernel-Source.git . --quiet
        
        # 快速配置
        echo "⚙️ 生成配置..."
        rm -f .config .config.old
        if ! make ${{ github.event.inputs.defconfig }}; then
          echo "⚠️ 使用备用配置"
          make defconfig
        fi
        
        # 关键优化：禁用调试信息，大幅减少编译量
        echo "🔧 应用编译优化..."
        ./scripts/config --disable DEBUG_INFO 2>/dev/null || true
        ./scripts/config --disable DEBUG_KERNEL 2>/dev/null || true
        ./scripts/config --disable GDB_SCRIPTS 2>/dev/null || true
        
        echo "⚡ 开始编译..."
        START_TIME=$(date +%s)
        
        # 分阶段编译，优先编译核心镜像
        echo "阶段1: 编译核心镜像 (vmlinux)"
        make -j$PARALLEL_JOBS vmlinux
        
        echo "阶段2: 编译内核镜像 (Image)"
        make -j$PARALLEL_JOBS Image
        
        echo "阶段3: 编译设备树 (dtbs)"
        make -j$PARALLEL_JOBS dtbs 2>/dev/null || true
        
        echo "阶段4: 编译模块 (modules)"
        make -j$PARALLEL_JOBS modules 2>/dev/null || true
        
        END_TIME=$(date +%s)
        COMPILE_TIME=$((END_TIME - START_TIME))
        
        echo ""
        echo "=== 🏁 编译完成 ==="
        echo "⏱️ 总耗时: ${COMPILE_TIME}秒"
        
        # 结果检查
        if ls arch/$ARCH/boot/Image* >/dev/null 2>&1; then
          echo "✅ 编译成功！"
          echo "生成文件:"
          find arch/$ARCH/boot/ -name "Image*" -type f -exec ls -lh {} \; | awk '{print "  " $9 " (" $5 ")"}'
          echo "BUILD_STATUS=success" >> $GITHUB_ENV
        else
          echo "❌ 未找到内核镜像"
          echo "BUILD_STATUS=no_image" >> $GITHUB_ENV
        fi
        
        echo "COMPILE_TIME=$COMPILE_TIME" >> $GITHUB_ENV

    - name: 上传产物
      if: env.BUILD_STATUS == 'success'
      uses: actions/upload-artifact@v4
      with:
        name: kernel-ultra-fast
        path: arch/arm64/boot/

    - name: 上传配置
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: kernel-config
        path: .config